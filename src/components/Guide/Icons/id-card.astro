<svg
  class="w-5 h-5"
  fill="none"
  stroke="currentColor"
  viewBox="0 0 24 24"
  xmlns="http://www.w3.org/2000/svg"
>
  <!-- Marco de la credencial -->
  <rect
    x="1"
    y="5"
    width="22"
    height="14"
    rx="2"
    ry="2"
    stroke-width="1.5"
    stroke-linecap="round"
    stroke-linejoin="round"></rect>

  <!-- Ícono de persona (sin círculo contenedor) -->
  <!-- Cabeza de la persona -->
  <circle
    cx="8"
    cy="9.5"
    r="1"
    stroke-width="1.2"
    stroke-linecap="round"
    stroke-linejoin="round"></circle>

  <!-- Cuerpo/hombros de la persona -->
  <path
    d="M6 12.5 Q6 11.5 8 11.5 Q10 11.5 10 12.5"
    stroke-width="1.2"
    stroke-linecap="round"
    stroke-linejoin="round"></path>

  <!-- Líneas de texto (nombre) -->
  <path d="M13 9 L20 9" stroke-width="1.5" stroke-linecap="round"></path>

  <!-- Líneas de texto (información) -->
  <path d="M13 11 L18 11" stroke-width="1.5" stroke-linecap="round"></path>

  <path d="M13 13 L20 13" stroke-width="1.5" stroke-linecap="round"></path>

  <!-- Línea de código de barras/chip -->
  <path d="M3 16 L21 16" stroke-width="1" stroke-linecap="round" opacity="0.6"
  ></path>
</svg>
