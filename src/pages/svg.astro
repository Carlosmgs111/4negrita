<svg class="w-20 h-20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
    <!-- Tarjeta de crédito (recortada) -->
    <path
      d="M3 8
         A2 2 0 0 1 5 6
         H19
         A2 2 0 0 1 21 8
         V14.8
         Q21 15.5 20.5 16.3
         L19 17"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"
    />
    
    <!-- Banda magnética -->
    <rect x="3" y="9" width="18" height="2" fill="currentColor" stroke="none" />
  
    <!-- Chip -->
    <rect x="5" y="13" width="3" height="2" rx="0.5" stroke-width="1" />
  
    <!-- Números cortados por la probeta -->
    <line x1="10" y1="13.5" x2="16" y2="13.5" stroke="currentColor" stroke-width="1" />
    <line x1="10" y1="15.5" x2="14" y2="15.5" stroke="currentColor" stroke-width="1" />
  
    <!-- Probeta (encima) -->
    <!-- Base triangular/cónica -->
    <path
      d="M16.5 21.5
         L19 17
         L21 17
         L23.5 21.5
         Z"
      stroke-width="1.2"
      stroke-linejoin="round"
    />
  
    <!-- Líquido interior -->
    <path
      d="M17.2 20.5
         L19 18
         L21 18
         L22.8 20.5
         Z"
      fill="currentColor"
      opacity="0.3"
      stroke="none"
    />
  
    <!-- Cuello doble -->
    <line x1="19" y1="17" x2="19" y2="15" stroke-width="1.2" />
    <line x1="21" y1="17" x2="21" y2="15" stroke-width="1.2" />
  
    <!-- Boca con reborde -->
    <path
      d="M18.5 15
         Q20 14.2 21.5 15"
      stroke-width="1.2"
      fill="none"
      stroke-linecap="round"
    />
  
    <!-- Burbuja -->
    <circle cx="20" cy="20" r="0.3" fill="currentColor" opacity="0.4" stroke="none" />
  </svg>
  